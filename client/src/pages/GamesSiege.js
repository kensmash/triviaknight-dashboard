import React from "react";
import GamesSiegeList from "../components/GamesSiegeList/GamesSeigeList";

const GamesSiege = ({ history, match }) => (
  <GamesSiegeList history={history} match={match} />
);

export default GamesSiege;
